return {
  -- Buffer navigation already lives on Shift-h/Shift-l and [b/]b. Removing the
  -- permanent tabline leaves more room for code without losing functionality.
  { "akinsho/bufferline.nvim", enabled = false },
}
