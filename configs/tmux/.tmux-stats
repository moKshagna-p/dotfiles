#!/bin/bash
iface=$(route get default 2>/dev/null | awk '/interface:/{print $2}')
ip=$(ifconfig "$iface" 2>/dev/null | awk '/inet /{print $2}')
load=$(sysctl -n vm.loadavg 2>/dev/null | awk '{print $2, $3, $4}')
[ -n "$ip" ] && printf '󰛳 %s 󰓅 %s' "$ip" "$load" || printf '󰓅 %s' "$load"
